# Caddyfile for Railway deployment
# Disable automatic HTTPS as Railway provides its own TLS termination
{
    auto_https off
}

# Serve the PHP application using FrankenPHP
:${PORT} {
    root * /app
    php_fastcgi unix//run/frankenphp.sock
    file_server
}
